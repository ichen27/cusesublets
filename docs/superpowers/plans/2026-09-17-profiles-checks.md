# Profiles and CuseSublets Checks implementation plan

Goal: publish new listings immediately without badges, independently manually review identity/lease/permission; polished host uploads; complete private account and transparent public profiles; completed-lease reviews.

Architecture: retain Worker/D1/private R2. Add profile/evidence/review tables with privacy-specific projections. One shared Checks component across listing, chat, host and public profile. Keep existing moderation and reservation verification gates. Do not mass-publish existing pending test or rejected listings. New submission is published by default; documentation upload resets only relevant badge, not publication. No raw ID or private contact fields in public responses.

1. Backend (worker/,migrations/,shared/types.ts,tests/,docs/API.md): extend own profile name/email read-only/phone/bio/social links/avatar/photos; public profile projected without contact/evidence; image uploads separately from ID documents. Manual identity queue and staff decisions with evidence version checks, reasons/audit; changing name clears identity review. Lease/permission review independently of publish status, evidence required before verification; preserve suspension. Public review eligibility requires real participant, completed end date, paid confirmed booking, no open dispute and unique booking/reviewer. No arbitrary self-reviews or date-only fabricated reviews. Test private evidence, auto-publication unverified, profile projection, review authorization.
2. Frontend (src/): replace profile with account editor, image gallery, private ID submission/review status; public profile with photos/socials/other listings/reviews + eligible review form. Shared Checks panel front and center, separate review labels. Host upload dropzone/file selector and modern review cards. Remove How it works header popup; footer trust link opens a full Checks explainer. Admin identity evidence review and separate listing moderation/review controls.
3. Verification: isolated worktree on mini, isolated runtime8921/state; meaningful API tests, typecheck/build/unit suite, independent review and desktop/mobile browser verification. Deploy only tested migration with live service stopped and state backup. Do not upload real personal ID as test. Preserve old unpublished test listing.
4. Save decisions, deployed state and limitations to Obsidian. Payments/legal signing remain unavailable as before. User has authorized these concrete changes; proceed with implementation without redundant design confirmation.
