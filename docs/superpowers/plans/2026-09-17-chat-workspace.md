# Chat workspace implementation plan

Goal: listing contact opens a full persistent conversation, which contains listing context, offers/counteroffers/requests, participant-only shared documents and booking next steps; hosts have an explicit My listings area.

Architecture: React ChatWorkspace and HostWorkspace alongside existing profile/admin. Worker conversation API using D1 and R2. Existing signed Google auth and private Access stay unchanged. Worktree /Volumes/SamsungSSD1/code/cusesublets-chat, branch feat/chat-workspace. Canonical deploy remains cusesublets. No live provider credentials exist; legal signing and money movement must remain unavailable in hosted mode, visibly explained in chat.

1. Backend (worker/, migrations/, shared/types.ts, tests/chat-runtime.py): persistent unique listing/buyer conversations, migration backfill from legacy messages and offers; participant-only reads/writes/document download; proposal actor and counteroffer lineage; atomic counter/accept status transitions; timeline events; no private review documents automatically shared. Endpoint contract supplied in backend task. Test using isolated state and 8920, never share existing runtime SQLite.
2. Frontend (src/ChatWorkspace.tsx, HostWorkspace.tsx, App.tsx, ListingDetail.tsx, styles.css): full chat opens directly from listing without requiring first message. Offer/request actions open same thread with composer. Summary property card and reopen detail link. Chat history, attachments, proposal cards and next-step signature/payment status. Responsive thread navigation. Host listings with review states, conversations/offers and reservations. Preserve current public map/filter behavior.
3. Review and verify backend authorization, file privacy/limits, counteroffer races and prior API compatibility; frontend typecheck/unit/build plus browser desktop/mobile and real staged account. Deploy migrations after stopping only CuseSublets, then merge tested changes/build/restart. Verify hosted guards and existing auth.
4. User property: await real listing details or create explicitly unpublished sample using prior example if user chooses test; never invent property for public launch. Associate with signed-in owner account, profile display name Ivan Chen. Update Obsidian and deployment/API docs with verified outcomes and provider limitations.

Execution: backend independent subagent alongside main frontend; independent review after integration. User has explicitly requested this behavior; no repeated approval gate. Existing private deployment updates are authorized; public access widening is not part of this change.
